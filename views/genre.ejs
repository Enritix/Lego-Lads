<%- include('components/header') %>
<%- include('components/nav') %>
<main>
  <div class="carousel-container">
    <h2 class="carousel-title">
      Kies een genre <span class="info-icon">&#x24D8;</span>
    </h2>
    <a href="/" class="back-button">❌</a>
    <section id="info-popup" class="popup">
      <article class="popup-content">
        <span class="close-button">❌</span>
        <p>Klik op het genre dat je wilt bekijken.</p>
      </article>
    </section>
    <div class="carousel">
      <button class="carousel-btn prev">❮</button>
      <div class="carousel-track">
        <% themes.forEach(function(theme, idx) { %>
          <a href="detaillist/<%= theme.id %>">
            <div class="carousel-item<%= idx === 0 ? ' active' : '' %>">
              <p class="carousel-alt"><%= theme.name %></p>
              <img src="<%= theme.img %>" alt="<%= theme.name %>" />
            </div>
          </a>
        <% }) %>
      </div>
      <button class="carousel-btn next">❯</button>
    </div>
  </div>
</main>
<%- include('components/settings') %>
</body>
</html>