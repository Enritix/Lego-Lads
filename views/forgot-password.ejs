<!DOCTYPE html>
<html lang="nl-BE">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Wachtwoord vergeten - LegoLads</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/login.css">
    <script src="/js/audio.js" defer></script>
</head>
<body>
    <audio id="clickSound">
        <source src="/assets/audio/kliksound.mp3" type="audio/mpeg">
    </audio>
    <main>
        <div class="resultaat-container">
            <a class="close" href="/landingspage">❌</a>
            <h2>Wachtwoord vergeten</h2>
            <% if (error) { %>
                <p style="color:red;"><%= error %></p>
            <% } %>
            <div class="registration-form">
                <% if (!showResetForm) { %>
                    <form action="/<%= lang %>/forgot-password" method="post">
                        <div class="form-group">
                            <label for="uname">Gebruikersnaam</label>
                            <input type="text" id="uname" name="uname" required>
                        </div>
                        <button class="text-lego-stroke" data-text="verzenden" type="submit">Verzenden</button>
                    </form>
                <% } else { %>
                    <form action="/<%= lang %>/forgot-password" method="post">
                        <input type="hidden" name="uname" value="<%= uname %>">
                        <div class="form-group">
                            <label for="password">Nieuw wachtwoord</label>
                            <input type="password" id="password" name="password" required>
                        </div>
                        <div class="form-group">
                            <label for="confirm-password">Herhaal wachtwoord</label>
                            <input type="password" id="confirm-password" name="confirm-password" required>
                        </div>
                        <button class="text-lego-stroke" data-text="opslaan" type="submit">Opslaan</button>
                    </form>
                <% } %>
            </div>
            <p>Weet je je wachtwoord weer? <a href="/login"><u>Log in</u></a></p>
        </div>
    </main>
</body>
</html>