export interface Achievement {

}

export interface Minifig {
    id: number;
    name: string;
    rarity: string;
    img: string;
}

export interface binElement {
    fig: string;
    reason: string;
}